from functools import reduce

# def isprime(num):
#     for n in range(2,int(num**0.5)+1):
#         if num%n==0:
#             return False
#     return True

#print(list(filter(isprime,range(2, int(input("Enter max: "))+1))))

values = [2,3,6,4,1,2]

def mul(currentTotal, item):
    return currentTotal * item

print(reduce(mul, values))